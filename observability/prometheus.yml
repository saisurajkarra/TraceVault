global:
  scrape_interval: 5s

scrape_configs:
  - job_name: tracevault
    # The FastAPI app runs on the host; scrape its /metrics endpoint.
    static_configs:
      - targets: ["host.docker.internal:8000"]
